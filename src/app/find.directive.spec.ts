import { FindDirective } from './find.directive';

describe('FindDirective', () => {
  it('should create an instance', () => {
    const directive = new FindDirective();
    expect(directive).toBeTruthy();
  });
});
